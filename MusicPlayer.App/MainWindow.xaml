<Window x:Class="MusicPlayer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MusicPlayer.App.ViewModels"
        Title="Music Player (SOLID)" Height="700" Width="900" Background="#1E1E1E" Foreground="White">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🎵 Music Player MVVM" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="↶ UNDO" Command="{Binding UndoCmd}" Margin="0,0,10,0" Padding="10,5" Background="#333" Foreground="White" BorderThickness="0"/>
                <Button Content="↷ REDO" Command="{Binding RedoCmd}" Padding="10,5" Background="#333" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="LIBRARY" FontSize="16" Foreground="Gray" Margin="0,0,0,10"/>
                <ListView ItemsSource="{Binding MainPlaylist.Tracks}" SelectedItem="{Binding SelectedTrack}" Height="200" Background="#2D2D30" Foreground="White" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Width="200"/>
                                <TextBlock Text="{Binding Duration, StringFormat={}{0:mm\\:ss}}" Foreground="LightGray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Content="+ Add Files..." Command="{Binding AddFilesCmd}" Margin="0,10,0,20" Padding="10" Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="Bold"/>

                <TextBlock Text="PLAYBACK MODE" FontSize="16" Foreground="Gray" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <RadioButton Content="Sequential" Command="{Binding ChangeStrategyCmd}" CommandParameter="Sequential" IsChecked="True" Foreground="White" Margin="0,0,10,0"/>
                    <RadioButton Content="Shuffle" Command="{Binding ChangeStrategyCmd}" CommandParameter="Shuffle" Foreground="White" Margin="0,0,10,0"/>
                    <RadioButton Content="Smart Shuffle" Command="{Binding ChangeStrategyCmd}" CommandParameter="SmartShuffle" Foreground="White" Margin="0,0,10,0"/>
                    <RadioButton Content="Repeat One" Command="{Binding ChangeStrategyCmd}" CommandParameter="RepeatOne" Foreground="White"/>
                </StackPanel>

                <TextBlock Text="STATISTICS" FontSize="16" Foreground="Gray" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Played:" Foreground="LightGray" Margin="0,0,0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalPlayedString}" FontWeight="Bold" Margin="0,0,0,5" Foreground="#007ACC"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Skips:" Foreground="LightGray"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Skips}" FontWeight="Bold" Foreground="#007ACC"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Border Width="150" Height="150" Background="#3E3E42" CornerRadius="10" Margin="0,0,0,20">
                    <TextBlock Text="♪♫" FontSize="50" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Player.CurrentTrack.Title, FallbackValue='No Track Selected'}" FontSize="24" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Player.State}" FontSize="16" Foreground="#007ACC" TextAlignment="Center" Margin="0,5,0,30"/>

                <TextBlock Text="ACTION HISTORY" FontSize="16" Foreground="Gray" Margin="0,0,0,10"/>
                <ListView ItemsSource="{Binding ActionHistory}" Height="120" Background="#2D2D30" Foreground="LightGray" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="2"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#2D2D30" CornerRadius="10" Padding="20" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="PREV" Command="{Binding PrevCmd}" Width="50" Height="40" Margin="0,0,10,0" Background="#444" Foreground="White" BorderThickness="0"/>
                    <Button Content="PLAY" Command="{Binding PlayCmd}" Width="70" Height="50" Margin="0,0,10,0" FontWeight="Bold" FontSize="14" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                    <Button Content="PAUSE" Command="{Binding PauseCmd}" Width="60" Height="40" Margin="0,0,10,0" Background="#444" Foreground="White" BorderThickness="0"/>
                    <Button Content="NEXT" Command="{Binding NextCmd}" Width="50" Height="40" Background="#444" Foreground="White" BorderThickness="0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0">
                    <Slider Minimum="0" Maximum="{Binding Player.CurrentTrack.Duration.TotalSeconds, FallbackValue=100}" Value="{Binding SeekPosition, Mode=TwoWay}" IsMoveToPointEnabled="True" Margin="0,0,0,5"/>
                    <Grid>
                        <TextBlock Text="{Binding Player.Position, StringFormat={}{0:mm\\:ss}}" HorizontalAlignment="Left" Foreground="Gray"/>
                        <TextBlock Text="{Binding Player.CurrentTrack.Duration, StringFormat={}{0:mm\\:ss}, FallbackValue='00:00'}" HorizontalAlignment="Right" Foreground="Gray"/>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="VOL" FontSize="12" Foreground="Gray" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <Slider Minimum="0" Maximum="1" Value="{Binding Player.Volume}" Width="100" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>